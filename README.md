# Nova

A small, focused AI assistant. Static site, no build step.

## Development

Just open `index.html`, or serve the folder with any static file server.

## Deployment

Deployed automatically to GitHub Pages on push to `main` via the workflow
in `.github/workflows/pages.yml`.
